#include <stdio.h>
int main(){

    // Arithmetic operators

    int x = 5;
    int y = 10;
    int n = 0;

    // printf("%d + %d is %d\n", x, y, (x+y));
    // printf("%d * %d is %d\n", x, y, (x*y));
    // printf("%d / %d is %d\n", x, y, (x/y));
    // printf("%d %% %d is %d\n", y, x, (y%x));
    // printf("%d - %d is %d\n", x, y, (x-y));

    // (X is 6, Y is 6)

    // int y = ++x; 

    // (X is 6, Y is 5)
    
    // int y = x++;

    // printf("X is %d, Y is %d\n", x, y);

   for (int i = 0; i < x;)
   {
      printf("I is %d\n", ++i);
   }

    printf("\n");

     for (int i = 0; i < x;)
   {
      printf("I is %d\n", i++);
   }  

}